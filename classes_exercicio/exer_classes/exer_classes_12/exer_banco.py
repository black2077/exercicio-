from abc import ABC,abstractmethodimport jsonclass Pessoa(ABC):    def __init__(self,nome,agencia,conta):        self.nome = nome        self.agencia = agencia        self.conta = conta    @abstractmethod    def sacar(self,conta):...    @abstractmethod    def depositar(self,conta,valor):...class Conta(Pessoa):    def __init__(self,conta_n):        self.conta_n = conta_n        self._saldo = 0        self._tipo_conta = 'PP'        with open('__lista.json','r') as file:            if    @property    def depositar(self):        return self._saldo    @depositar.setter    def depositar(self,valor):        self._saldo = valor    @property    def sacar(self):        return self._saldo    @sacar.setter    def sacar(self,valor:int):        if  self._saldo >= valor:            self._saldo = self._saldo - valor        else:            print('__saldo insulficiente')    def __str__(self):        return f'{__class__.__name__} = {self.conta_n} {self._saldo}'pesss = Conta('564656')pesss.depositar = 98print(pesss.depositar)print()pesss.sacar = 90print(pesss.sacar)